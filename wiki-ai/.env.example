# Absolute or relative to wiki-ai/
VAULT_ROOT=../LLM_Wiki
HOST=127.0.0.1
PORT=8787

# 32-byte key, base64-encoded (generate: python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())")
CREDENTIAL_KEY_BASE64=

# Optional default OpenAI-compatible base URL when user leaves it blank
DEFAULT_BASE_URL=https://api.deepseek.com/v1
DEFAULT_MODEL=deepseek-chat
