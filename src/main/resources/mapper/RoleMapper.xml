<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fjnu305.acm01.module.user.mapper.RoleMapper">

    <select id="selectByRoleCode" resultType="org.fjnu305.acm01.module.user.entity.RoleEntity">
        SELECT id, role_code AS roleCode, role_name AS roleName, status, created_time AS createdTime
        FROM role
        WHERE role_code = #{roleCode} AND status = 1
        LIMIT 1
    </select>

    <select id="selectRoleCodesByUserId" resultType="string">
        SELECT r.role_code
        FROM user_role ur
        INNER JOIN role r ON ur.role_id = r.id
        WHERE ur.user_id = #{userId} AND r.status = 1
    </select>

    <insert id="insertUserRole">
        INSERT INTO user_role (user_id, role_id)
        VALUES (#{userId}, #{roleId})
    </insert>

    <select id="countUsers" resultType="int">
        SELECT COUNT(1) FROM user WHERE deleted = 0
    </select>
</mapper>
