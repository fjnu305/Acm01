<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fjnu305.acm01.module.contest.crawl.mapper.ContestSourceMapper">

    <update id="updateOnSuccess">
        UPDATE contest_source
        SET last_crawl_time   = NOW(),
            last_crawl_status = 'SUCCESS',
            fail_count        = 0
        WHERE source_code = #{sourceCode}
    </update>

    <update id="updateOnFailure">
        UPDATE contest_source
        SET last_crawl_time   = NOW(),
            last_crawl_status = 'FAILED',
            fail_count        = fail_count + 1
        WHERE source_code = #{sourceCode}
    </update>

    <select id="selectCrawlEnabled" resultType="java.lang.Integer">
        SELECT crawl_enabled
        FROM contest_source
        WHERE source_code = #{sourceCode}
    </select>

    <select id="selectRateLimitSec" resultType="java.lang.Integer">
        SELECT rate_limit_sec
        FROM contest_source
        WHERE source_code = #{sourceCode}
    </select>
</mapper>
