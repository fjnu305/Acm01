<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fjnu305.acm01.module.team.mapper.TeamMemberLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO team_member_log (team_post_id, user_id, action, actor_id)
        VALUES (#{teamPostId}, #{userId}, #{action}, #{actorId})
    </insert>

    <insert id="log">
        INSERT INTO team_member_log (team_post_id, user_id, action, actor_id)
        VALUES (#{teamPostId}, #{userId}, #{action}, #{actorId})
    </insert>
</mapper>
