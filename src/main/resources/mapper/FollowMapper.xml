<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fjnu305.acm01.module.social.mapper.FollowMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO follow (follower_id, followee_id)
        VALUES (#{followerId}, #{followeeId})
    </insert>

    <delete id="delete">
        DELETE FROM follow WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </delete>

    <select id="exists" resultType="boolean">
        SELECT COUNT(1) > 0 FROM follow
        WHERE follower_id = #{followerId} AND followee_id = #{followeeId}
    </select>

    <select id="selectFolloweeIds" resultType="long">
        SELECT followee_id FROM follow WHERE follower_id = #{followerId}
        ORDER BY created_time DESC
    </select>

    <select id="selectFollowerIds" resultType="long">
        SELECT follower_id FROM follow WHERE followee_id = #{followeeId}
    </select>
</mapper>
