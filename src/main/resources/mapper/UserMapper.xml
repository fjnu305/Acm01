<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.fjnu305.acm01.module.user.mapper.UserMapper">

    <resultMap id="UserResultMap" type="org.fjnu305.acm01.module.user.entity.UserEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="email" column="email"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="school" column="school"/>
        <result property="bio" column="bio"/>
        <result property="cfHandle" column="cf_handle"/>
        <result property="atcoderHandle" column="atcoder_handle"/>
        <result property="nowcoderHandle" column="nowcoder_handle"/>
        <result property="luoguHandle" column="luogu_handle"/>
        <result property="cfRating" column="cf_rating"/>
        <result property="solvedCount" column="solved_count"/>
        <result property="acCount" column="ac_count"/>
        <result property="contestCount" column="contest_count"/>
        <result property="status" column="status"/>
        <result property="lastLoginTime" column="last_login_time"/>
        <result property="createdTime" column="created_time"/>
        <result property="updatedTime" column="updated_time"/>
        <result property="deleted" column="deleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, username, password, nickname, email, avatar, gender, school, bio,
        cf_handle, atcoder_handle, nowcoder_handle, luogu_handle,
        cf_rating, solved_count, ac_count, contest_count,
        status, last_login_time, created_time, updated_time, deleted
    </sql>

    <select id="selectByUsername" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE username = #{username} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectById" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE id = #{id} AND deleted = 0
        LIMIT 1
    </select>

    <select id="selectByIds" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE deleted = 0 AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(1)
        FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, nickname, email, status, deleted)
        VALUES (#{username}, #{password}, #{nickname}, #{email}, #{status}, 0)
    </insert>

    <update id="updateLastLoginTime">
        UPDATE user
        SET last_login_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateProfile">
        UPDATE user
        SET nickname = #{nickname},
            email = #{email},
            school = #{school},
            bio = #{bio},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateAvatar">
        UPDATE user
        SET avatar = #{avatar},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectPage" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%')
                OR nickname LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY created_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(1)
        FROM user
        WHERE deleted = 0
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%')
                OR nickname LIKE CONCAT('%', #{keyword}, '%')
                OR email LIKE CONCAT('%', #{keyword}, '%'))
        </if>
    </select>

    <update id="updateStatus">
        UPDATE user
        SET status = #{status},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="countTodayRegistrations" resultType="int">
        SELECT COUNT(1)
        FROM user
        WHERE deleted = 0 AND DATE(created_time) = CURDATE()
    </select>

    <update id="updateCfRating">
        UPDATE user
        SET cf_rating = #{cfRating},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateCfHandle">
        UPDATE user
        SET cf_handle = #{cfHandle},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <update id="updateContestCount">
        UPDATE user
        SET contest_count = #{contestCount},
            updated_time = NOW()
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectActiveUserIds" resultType="long">
        SELECT id
        FROM user
        WHERE deleted = 0
          AND status = 1
    </select>

    <select id="selectSearchCandidates" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user
        WHERE deleted = 0
          AND status = 1
          AND id &lt;&gt; #{excludeUserId}
          AND (
              username LIKE CONCAT('%', #{keyword}, '%')
              OR nickname LIKE CONCAT('%', #{keyword}, '%')
          )
        ORDER BY
            CASE
                WHEN username = #{keyword} OR nickname = #{keyword} THEN 0
                WHEN username LIKE CONCAT(#{keyword}, '%') OR nickname LIKE CONCAT(#{keyword}, '%') THEN 1
                ELSE 2
            END,
            created_time DESC
        LIMIT #{limit}
    </select>
</mapper>
